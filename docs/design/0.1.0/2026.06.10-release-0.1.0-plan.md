# Release 0.1.0 — Detailed Plan (Arc 04 + Arc 05)

**Date:** 2026.06.10 · **Branch:** `release/0.1.x` · **Parent:**
`2026.06.10-remediation-program-plan.md` · **Framework:** ledger discipline per
slice (CC implements / CDC verifies; grep-verifiable rows; five-iteration cap).

This is the slice-level plan for the two 0.1.0 arcs. Each slice below gives:
*scope* (the end-to-end change), *ledger-seed* (acceptance criteria that become
the grep-verifiable rows), *files*, and a *cc-prompt skeleton*. The ledger-seeds
are formalised into a real `ledger.md` when the slice is picked up; slice 01.01's
is already written (`2026.06.10-slice-01.01-stabilize-ledger.md`) as the starter.

Origin tags (`L-NN`) trace every criterion back to `2026.06.10-audit-results-lfe.md`.

---

## Arc 04 — Stabilise the branch

**Arc goal:** `release/0.1.x` is a known-good baseline — compiles, tests green,
tree clean, design docs sorted — *before* any behavioural fix lands. Nothing here
changes runtime behaviour; it's the floor the Arc 05 fixes stand on.

### Slice 01 — Working state + repo hygiene

**Scope.** Bring the branch to green and clean the tree. Triage the crash dump
*before* deleting it (it may reproduce L-01 or L-04 — free evidence). Confirm the
build and test commands actually pass on a clean checkout; fix whatever's needed
to make them pass (compile errors, test breakage) — but *no audit behavioural
fixes* (those are Arc 05). Decide the fate of the untracked
`xrepl-protocol-impl-master-plan.md` (track it or remove it).

**Ledger-seed (criteria → rows):**

- F-1 `rebar3 compile` exits 0 on a clean checkout of `release/0.1.x`.
  *(serious; Origin: working-state)*
- F-2 `rebar3 as test lfe ltest` runs and is green. *(serious; working-state)*
- F-3 `erl_crash.dump` is triaged (one-paragraph note in the closing report:
  what crashed, whether it maps to a known finding) **then** removed from the
  tree. *(correctness; L-21)*
- F-4 `test_quit.lfe`, `tmp/`, `COMMIT_MSG.txt` removed or relocated out of the
  repo root. *(polish; L-21)*
- F-5 `xrepl-protocol-impl-master-plan.md` is either `git add`-ed or removed; no
  untracked design doc remains. *(polish; L-21)*
- F-6 `git status --short` shows no stray untracked files except the intended
  `workbench/`. *(polish; L-21)*

**Files.** repo root, `_build/` (ignored), `docs/design/`. No `src/` behaviour
changes.

**cc-prompt skeleton.**
> Branch `release/0.1.x`. Goal: known-good baseline, no behavioural changes.
> (1) Run `rebar3 compile` and `rebar3 as test lfe ltest`; make them green —
> fix only what blocks build/test, not audit findings. (2) Open `erl_crash.dump`,
> summarise the crash in one paragraph in the closing report, note whether it
> matches L-01 (keepalive) or L-04 (session cleanup), then delete it. (3) Remove
> `test_quit.lfe`, `tmp/`, `COMMIT_MSG.txt`; resolve the untracked
> `xrepl-protocol-impl-master-plan.md`. Close every ledger row with the exact
> command output as evidence. Do **not** touch network/session behaviour.

### Slice 02 — Design-doc archaeology → `docs/design/0.1.0/`

**Scope.** Read each of the 18 docs in `docs/design/`, classify *completed*
(describes Phase 1/2/3 work that shipped) vs *forward-looking* (vision/spec not
yet built), and `git mv` the completed ones into `docs/design/0.1.0/`. **Verify
status by reading the doc against the code — do not classify from the filename.**
The proposed split below is a *starting hypothesis* the slice confirms or
corrects.

*Proposed completed → `docs/design/0.1.0/`:* `xrepl-phase1-prompt.md`,
`xrepl-phase1-cli-addendum.md`, `xrepl-phase2-prompt.md`, `xrepl-phase3-prompt.md`,
`xrepl-phase3-v2.md`, `xrepl-phase3-v3.md`,
`lfe-repl-reusability-analysis-xrepl-design.md`, `xrepl-protocol-extraction.md`
(protocol lib exists), `terminal-graphics-support.md` + `xrepl-graphics-impl.md`
(graphics shipped in `xrepl-term`), `xrepl-history-with-session.txt`,
`xrepl-transport-improvements.txt` (confirm — may be partial).

*Proposed forward-looking → stay in `docs/design/`:* `xrepl-graphics-vision.md`
(Vega-Lite, unbuilt), `planning-reveal-like-viz-in-xrepl.md`,
`distributed-repl-architecture-guide.md`, `xrepl-unified-spec.md` (80+ ops,
mostly future), `xrepl-emacs-guide.md`, `xrepl-protocol-impl-master-plan.md`.

**Ledger-seed:**

- F-1 `docs/design/0.1.0/` exists and contains every doc classified completed.
  *(polish; def-of-done #3)*
- F-2 Each move is `git mv` (history preserved), verified by
  `git log --follow` resolving on a moved file. *(polish)*
- F-3 The closing report lists all 18 docs with a one-line completed/forward
  disposition **and the evidence** for the call (e.g. "completed — `xrepl-term`
  implements the iTerm2/Kitty path this doc specifies"). *(correctness; spec-keeping)*
- F-4 No doc is moved whose feature is unbuilt (no false "completed"); spot-check
  by grepping the code for the feature each "completed" doc claims. *(correctness)*

**Files.** `docs/design/**` only.

**cc-prompt skeleton.**
> Classify the 18 docs in `docs/design/` as completed-vs-forward by **reading
> each against the code** (not the filename). `git mv` the completed ones into
> `docs/design/0.1.0/`. In the closing report, give every doc a one-line
> disposition with its evidence. If a doc is ambiguous, leave it in
> `docs/design/` and say why. The proposed split in the slice plan is a
> hypothesis to confirm, not an instruction to follow blindly.

**Arc 04 checkpoint.** Branch green, tree clean, docs sorted. Arc 05 may begin.

---

## Arc 05 — 0.1.0 critical correctness (the limited cut)

**Arc goal:** the deliberately small set of fixes that make 0.1.0 *useful and
correct* in normal use. Each slice is one finding, one mergeable diff, one
ledger — small enough to ship or revert independently. After the last slice
closes, Duncan re-tags 0.1.0.

> **Why these four (+1 swing) and not more.** The default 0.1.0 experience is the
> local stdio REPL (`network_enabled=false`). L-04 and L-05 corrupt/leak state in
> *local* multi-session use; L-09 is a *local* foot-gun; L-01 breaks the network
> mode the README/launcher advertise. Those four make the shipped thing wrong.
> Everything else is structural (needs the Arc 05 refactors) or doesn't affect
> normal use — disclosed-deferred per the program plan §7. L-02 is the swing
> (program plan §5).

### Slice 01 — L-01: kill the idle-keepalive desync

**Scope.** Remove the `(after 30000 …)` keepalive that injects an unsolicited
`#m(status ping)` frame into a synchronous request/response stream
(`xrepl-tcp-handler.lfe:79-82`, `send-keepalive` at `:167-171`). A REPL connection
doesn't need app-level keepalives. **Also** the doc-truth check (def-of-done #5):
confirm README/`bin/xrepl` network claims are honest once this lands. Add a
regression test: connect, idle past the old 30s window, eval, assert a correct
reply (not a `ping`/`case_clause`).

**Ledger-seed:**

- F-1 the `after 30000` clause is gone from `message-loop`; `grep -n "after 30000"
  src/xrepl-tcp-handler.lfe` returns nothing. *(serious; L-01)*
- F-2 `send-keepalive` is removed or unreferenced (`grep -rn send-keepalive src/`
  → only a definition that's dead, or nothing). *(correctness; L-01)*
- F-3 a test connects, waits past the former keepalive interval (or simulates an
  idle tick), evals, and asserts a `done`/`error` reply — **fails** if a `ping`
  frame is injected. *(serious; L-01 / L-07-style regression)*
- F-4 `xrepl-client:eval` still has no `ping` clause **and no longer needs one** —
  documented in the closing report. *(correctness; L-01)*

**Files.** `src/xrepl-tcp-handler.lfe`; a new test under `test/`; possibly
`README.md`/`bin/xrepl` wording.

### Slice 02 — L-04: route session expiry through the lifecycle

**Scope.** `do-cleanup-expired-sessions` (`xrepl-store.lfe:443-472`) currently
deletes the ETS row of an expired session whose process is still alive *without
stopping the process* — orphaning the session gen_server, its evaluator, and its
history ETS table, and leaving the store/registry inconsistent. Fix: expiry must
stop the process through the lifecycle owner (`xrepl-session-manager:destroy/1`,
which stops via `xrepl-session-sup` then deletes the row) rather than reaching
into ETS + the registry directly. (Does *not* require the L-20 dedup — that's
Arc 05; this slice fixes the leak against the current code.)

**Ledger-seed:**

- F-1 after an expiry sweep, a session that was alive has **no** registered
  process: `whereis(process-name)` → `undefined`. *(serious; L-04)*
- F-2 its history ETS table is gone: `ets:info(table-name)` → `undefined`.
  *(serious; L-04)*
- F-3 store and registry agree post-sweep — no id for which `is-active?` is true
  but `get-session` is `not-found`. *(serious; L-04)*
- F-4 a test creates a session, forces expiry (short timeout or direct call), and
  asserts F-1–F-3. *(serious; L-04)*
- F-5 cleanup no longer calls `ets:delete` directly on a live session's row; it
  goes through `xrepl-session-manager:destroy` (or equivalent lifecycle call).
  *(correctness; L-04)*

**Files.** `src/xrepl-store.lfe` (and/or move expiry into
`xrepl-session-manager.lfe`); a test under `test/`.

### Slice 03 — L-05: per-session history + `terminate/2` save

**Scope.** Today every session's `setup-save-on-exit` spawns an unsupervised
process that monitors the global `user` and saves *its* history to the **shared**
`~/.lfe-xrepl-history`, so concurrent sessions clobber each other (data loss) and,
in `--server` mode (no `user`), the spawned process leaks forever. Fix: (a)
persist per-session to a per-session path (e.g. `~/.xrepl/history/<id>`); (b) tie
the save to the session gen_server's `terminate/2` (it already traps exits) and
delete the `setup-save-on-exit` spawn entirely.

**Ledger-seed:**

- F-1 `setup-save-on-exit` and its `spawn` are gone:
  `grep -n setup-save-on-exit src/xrepl-history.lfe` → nothing. *(serious; L-05)*
- F-2 save happens in `xrepl-session:terminate/2`
  (`grep -n "terminate" src/xrepl-session.lfe` shows the save call). *(correctness; L-05)*
- F-3 two concurrent sessions with distinct histories both persist without
  clobber — a test creates two, exits both, asserts each file has its own
  commands. *(serious; L-05)*
- F-4 no per-session saver process survives a `--server`-style start with no
  `user` (no leaked process). *(correctness; L-05)*
- F-5 history file path is per-session, not the shared default for writes:
  evidenced by the path used in `terminate/2`. *(correctness; L-05)*

**Files.** `src/xrepl-history.lfe`, `src/xrepl-session.lfe`; a test.

*Note:* this changes the on-disk history layout. If 0.1.0 must read the old
single-file history, add a one-time import; otherwise document the break in the
closing report. Flag for Duncan if back-compat matters.

### Slice 04 — L-09: identify ids by shape, not hex-parseability

**Scope.** `is-session-id?` (`xrepl-session-manager.lfe:357-370`) and
`resolve-session-id` (`xrepl-commands.lfe:239-255`) decide "is this an id?" by
`list_to_integer(Str, 16)`, so any hex-ish *name* (`"face"`, `"42"`, `"cafe"`) is
misclassified as an id and name-lookup is skipped. Fix: an id is exactly 32 hex
chars; anything else is a name (or carry an explicit `{id,_}`/`{name,_}` tag).

**Ledger-seed:**

- F-1 a session named `"face"` resolves by name: a test creates it, then
  `switch-session "face"` / `resolve-session-id "face"` returns its id, not
  `not-found`. *(serious; L-09)*
- F-2 a 32-hex-char string still resolves as an id. *(correctness; L-09)*
- F-3 the discriminator is shape-based: `grep -n "list_to_integer"
  src/xrepl-session-manager.lfe src/xrepl-commands.lfe` shows it's no longer the
  id/name decision (or is gone). *(correctness; L-09)*
- F-4 both call sites (`is-session-id?` and `resolve-session-id`) use the same
  rule — no partial adoption. *(correctness; L-09 / partial-adoption check)*

**Files.** `src/xrepl-session-manager.lfe`, `src/xrepl-commands.lfe`; a test.

### Slice 05 — L-02: bound intake (swing — see program plan §5)

**Scope.** Add `{packet_size, N}` to the socket opts in
`xrepl-tcp-handler.lfe:36-38` so a hostile 4-byte length can't make the driver
buffer gigabytes pre-auth, and pass `max_connections` into the Ranch
`transport_opts` in `xrepl-net-sup.lfe:47`. **Include only if Duncan greenlights
for 0.1.0** (default: include); otherwise this slice moves to Arc 05.

**Ledger-seed:**

- F-1 `grep -n "packet_size" src/xrepl-tcp-handler.lfe` shows a finite cap set
  alongside `{packet, 4}`. *(serious; L-02)*
- F-2 an oversized length-prefixed frame is rejected without unbounded buffering —
  a test sends a frame claiming a huge length and asserts the connection errors/
  closes rather than hanging. *(serious; L-02)*
- F-3 `max_connections` from app env reaches the Ranch `transport_opts`
  (`grep -n max_connections src/xrepl-net-sup.lfe`). *(correctness; L-02 / L-06-adjacent)*

**Files.** `src/xrepl-tcp-handler.lfe`, `src/xrepl-net-sup.lfe`; a test.

**Arc 05 checkpoint → TAG 0.1.0.** All slices closed with CDC-verified ledgers;
def-of-done (program plan §4) met. Duncan re-tags `0.1.0`. Then rebase main off of release/0.1.x and cut
`release/0.2.x` from the updated main.

---

## Notes for the executor (CC) and reviewer (CDC)

- **One slice = one branch off `release/0.1.x` = one ledger.** Don't bundle.
- **Tests are the Verify column where behaviour changes.** A grep proves code
  *shape*; a test proves *behaviour*. L-01/L-04/L-05/L-09/L-02 each need a test
  that would fail if the bug were still present — not just a grep that the code
  moved.
- **CDC re-runs every `done` row's Verify command.** A `done` without reproduced
  evidence is `proposed-done`.
- **Arc 05 ordering is independent** (the four core slices don't depend on each
  other), so they can be parallelised across branches if desired — but each still
  rebases onto a green `release/0.1.x` and closes its own ledger.
- **Back-compat flags:** slice 02.03 changes the history on-disk layout — surface
  to Duncan before closing if old-history import matters.
